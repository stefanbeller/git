@@ @@
 git_path_cherry_pick_head(
+the_repository
 )

@@ @@
 git_path_revert_head(
+the_repository
 )

@@ @@
 git_path_squash_msg(
+the_repository
 )

@@ @@
 git_path_merge_msg(
+the_repository
 )

@@ @@
 git_path_merge_rr(
+the_repository
 )

@@ @@
 git_path_merge_mode(
+the_repository
 )

@@ @@
 git_path_merge_head(
+the_repository
 )

@@ @@
 git_path_fetch_head(
+the_repository
 )

@@ @@
 git_path_shallow(
+the_repository
 )
